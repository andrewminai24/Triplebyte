def find(nums):
    max_num = float('-inf') # smaller tha
    #other numbers
    for nums in nums:
            if num > max_num:
                #fill in the missing line here
        return max_num

    print(max_num)
